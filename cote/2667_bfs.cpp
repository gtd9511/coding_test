#include <iostream>
#include <vector>

using namespace std;

bool visited [25][25] = { false };

void dfs(int x, int y)
{
	visited[x][y] = true;

	if (!visited[x][y] && )

}

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);

	return (0);

}
